# Global Quality Gates

- Run deterministic checks relevant to the change surface.
- Fail fast on regressions in tests, lint, type checks, or contract checks.
- Do not mark tasks complete without a validation summary.
- Keep CI parity with local validation commands.
- Add or update tests when behavior changes.
