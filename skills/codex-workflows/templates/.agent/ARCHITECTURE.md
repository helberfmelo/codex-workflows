# Codex Workflow Template Architecture

This template provides local workflow files for projects that do not have `.agent` yet.

- `workflows/`: execution playbooks
- `rules/`: global rules for local project behavior
- `scripts/`: helper scripts for validation and local automation
